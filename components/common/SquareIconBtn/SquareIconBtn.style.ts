import { IconButton, styled } from '@mui/material'

const StyledSquareIconBtn = styled(IconButton)(({ theme }) => ({
  border: '1px solid #dae2ed',
  borderRadius: '12px',
}))

export default StyledSquareIconBtn
